function HerderComponent({titulo}) {

    return (
        <header className="my-5 ms-1 border border-dark row">
            <h1>{titulo}</h1>
        </header>  
    );
}

export default HerderComponent;